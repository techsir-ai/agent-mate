# 网络层适配想法-0819

> agent-mate 4.1《网络环境管理》需求设计讨论定稿（2026-08-19）
> 核心：命令执行前内存层面拦截/改写网络请求，国内网络动态静默选源，不落盘不留痕。

---

## 0. 术语基座（260819 凌晨定稿）

- **目标实体** → **平台** → **访问方式** → **通道** → **端点**
- **通道优先级（铁律）**：1 代理 → 2 直达 → 3 镜像站 → 4 加速器；1/2 固定位不入表
- **绑死规则**：版本探测 + 下载同平台同通道；git 用 latest；包管理器源不探测

---

## 1. 拦截改写模型（核心认知）

### 1.1 拦截点

- 命令行与 GUI 按钮**都是意图明确**：`意图明确 → 执行前介入 → 查表选源 → 改写/构造 → 执行`
- 命令行多一道工序：**文本解析成结构化意图**；GUI 按钮本身就是结构化意图，无需解析
- 拦截发生在**「回车/点击之后、创建子进程之前」**，数据全程在 agent-mate 内存

### 1.2 协议模型（无 MITM、无证书）

- 拦截对象是 HTTP GET（明文），请求行里 URL 直接可见
- 流程：**捕获（拿到明文 URL）→ 改写（请求行 + Host）→ 执行（由 agent-mate 发出）**，全流程无加密参与
- **与代理/证书/MITM 无关**：改写是内存中的字符串操作
- **无痕保证**：不装证书；代理设置运行期改、退出恢复（或进程级环境变量）；规则路由全内存
- **边界**：不读系统代理的客户端需 TUN（驱动 = 痕，默认不做）

---

## 2. 拦截改写方法分类（A / B 两大类）

### A 类：拦截改写（针对命令与脚本）

| 子类 | 机制 | 覆盖场景 | 备注 |
|---|---|---|---|
| **A2 PATH wrapper（主）** | PATH 前插入 agent-mate 目录，放 curl/wget 包装器；脚本内部子命令执行时被拦截，wrapper 拿到**变量展开后的完整 URL** | bash 安装脚本内部所有外部 curl/wget 下载 | 主方案，覆盖 90% 脚本场景 |
| **A1 文本层兜底（可选）** | 用户直接敲的绝对路径命令（如 `/usr/bin/curl`）绕过 PATH | 显式绝对路径 | 可选，不做也行 |

### B 类：注入（针对进程内部请求与配置口）

| 子类 | 机制 | 覆盖场景 |
|---|---|---|
| **B1 环境变量** | 注入 `npm_config_registry` / `PIP_INDEX_URL` / `UV_INDEX_URL` / `UV_PYTHON_INSTALL_MIRROR` / git `insteadOf` 等 | 包管理器、工具链进程内部的请求；A2 拦不到的所有软件自有下载逻辑 |
| **B2 解释器钩子** | PYTHONPATH 注入 sitecustomize.py monkey-patch urllib/requests；PowerShell profile 覆盖 cmdlet | pwsh 脚本内部 `irm`/`Invoke-WebRequest`（不走 PATH，A2 全盲区） |

### 方法优先级

```
A2（PATH wrapper）+ B1（环境变量）为主力 → B2 仅 pwsh 场景兜底 → A1 可选
```

### 真盲区（需明确记录）

- **bash 函数覆盖 curl**：函数优先于 PATH，wrapper 拦不到，静态分析不可靠 → 只能靠 B1 或文本层
- **PowerShell cmdlet**：`irm` / `Invoke-WebRequest` / `Invoke-RestMethod` 不走 PATH → 只能 B2 钩子或改写脚本

---

## 3. 实测验证（hermes 官方安装脚本）

> 样本：NousResearch/hermes-agent `scripts/install.sh`（3541 行）+ `install.ps1`（4834 行），GitHub main 分支原版

### 3.1 结论

- **两个脚本均无 python 直接下载**（Python 一律经 `uv python install`）、**无函数覆盖 curl**
- bash 版下载 **100% 走外部 curl** → **A2 全覆盖**
- pwsh 版下载 **100% 走 cmdlet** → **A2 全盲区，必须 B2**

### 3.2 脚本内网络请求清单（全部有国内镜像）

| 目标 | bash 方式 | pwsh 方式 | 拦截手段 | 国内镜像 |
|---|---|---|---|---|
| nodejs.org 二进制 | 外部 curl | Invoke-WebRequest | A2 / B2 | npmmirror.com/mirrors/node |
| github.com git clone | 外部 git | — | A2 git wrapper 或 B1 insteadOf | GitHub CDN 加速（ghproxy 类） |
| raw.githubusercontent.com（cua-driver） | 外部 curl | Invoke-RestMethod | A2 / B2 | GitHub CDN raw 加速 |

### 3.3 uv 三层（唯一特殊目标）

| 层 | 请求位置 | 拦截手段 | 国内镜像 |
|---|---|---|---|
| 安装 uv（下载安装器脚本） | **脚本里**外部 curl/irm | A2 / B2，改写 astral.sh | cnrio.cn/install.sh → cnb.cool |
| uv python install（python 二进制） | **uv 进程内**下载 python-build-standalone | 拦不到，只能 B1 `UV_PYTHON_INSTALL_MIRROR` | cnb.cool（腾讯云） |
| uv pip / 自更新 | uv 内部访问 PyPI / astral 资源 | 只能 B1 `UV_INDEX_URL` / `UV_ASTRAL_MIRROR_URL` / `UV_DOWNLOAD_URL` | PyPI 清华/阿里/腾讯镜像；astral 资源 cnb.cool |

### 3.4 cnrio.cn/install.sh 判定

- 与官方 astral-sh/uv 0.12.5 **同模板**（cargo-dist 0.31.0），仅两处改动：默认源 → cnb.cool + 尾部 CNB 段落
- **版本/功能零修改**，带 sha256 校验，可完整替代官方 install.sh
- 副作用：CNB 私货会写 `UV_PYTHON_INSTALL_MIRROR` / `UV_ASTRAL_MIRROR_URL` 到 ~/.profile/.bashrc/.zshrc
- **无痕用法**：`UV_NO_MODIFY_PATH=1 UV_DISABLE_UPDATE=1`（hermes 脚本未传前者，需 agent-mate 注入）

---

## 4. 两层声明式规则架构（定稿）

### 4.1 分层定义

| 层 | 作用域 | 内容 | 变更频率 |
|---|---|---|---|
| **第一层（global）** | 所有 agent | 开发三件套（nodejs/python/git）安装包 + 包管理源（npm/pip）+ 源改写 + uv + GitHub CDN 基础设施 | 低（镜像失效、版本路径变化） |
| **第二层（agent）** | 单个 agent | 该 agent 安装脚本在**第一层覆盖不到处**的补丁/加固（如 astral.sh 改写、UV_PYTHON_INSTALL_MIRROR、pwsh B2 钩子） | 高（新 agent、版本升级适配） |

- 运行时同一引擎处理两级规则，**第二层优先级高于第一层**（per-agent 覆盖全局）
- 新增 agent 成本 = 分析安装脚本 → 清单划归已覆盖项 → 只写没覆盖的补丁

### 4.2 统一 schema（两层合并）

- 两层**同一 schema**，靠 `scope` 字段区分：`global` vs `agent:<name>`
- 规则形态 = **声明式规则数据**（目标实体表：目标 → 改写规则 → 路线表 → 校验），引擎解释执行
- **禁任意可执行脚本**：公共站被攻破最多改错 URL，不允许 RCE
- 适配粒度：`agent名@版本号` → 规则集；agent 升级 → 新适配版本 → 客户端下次启动自动更新

### 4.3 软件本体

```
软件本体 = 纯引擎（解析 / 校验 / 同步 / 改写 / 注入）+ 出厂兜底规则（最小集）
```

- 出厂兜底规则 = 三件套默认源改写（npmmirror / PyPI 镜像 / GitHub CDN），保证首次启动、断网、公共站全挂时最基本安装可用
- 软件本体只在引擎修 bug 时发版；**一切规则变化走动态更新**

### 4.4 启动时增量同步机制

```
软件启动
  └─ 拉公共站 manifest.json（版本清单，很小）
       ├─ 对比本地缓存版本
       │    ├─ 无变化 → 直接用本地缓存（离线可用）
       │    └─ 有新版本 → 只拉变更的规则集 → sha256 校验 → 原子替换
       └─ 公共站不可达 → 静默降级
```

| 组件 | 作用 | 要求 |
|---|---|---|
| manifest.json | 版本清单（两层分开版本号：agent 名 + 适配版本 + 校验值） | **增量拉取**，启动只拉 manifest 不拉全量 |
| sha256 校验 | 防公共站篡改 | **必须**，校验不过不加载 |
| 本地缓存 | 公共站挂也能用旧规则 | 启动先本地后远端 |
| 版本化文件名 | 沿用 `260816` 式版本命名 | 备份后覆盖流程（规则 5.5/5.6） |

**降级链路**：公共站 → 最近一次成功缓存 → 出厂兜底规则

### 4.5 发布策略（机制统一，策略分层）

| 通道 | 适用 | 门槛 | 出错影响 |
|---|---|---|---|
| **stable** | 第一层（global） | 人工审 + 版本门禁 + 可回滚 | 全量安装失败 |
| **fast** | 第二层（agent） | 快速迭代 | 单 agent 适配失败 |

### 4.6 公共站选址

约束：国内可达 + 静态托管。候选：GitHub Pages（需 CDN 加速）、Gitee Pages、腾讯云 COS 静态站、CloudStudio 静态托管。manifest + 规则集全是静态文件，任何静态托管都能干。

---

## 5. 安全边界（硬性）

1. **第二层必须是声明式规则，不是任意脚本**——公共站被攻破 ≠ RCE
2. **sha256 校验必须**，校验不过不加载
3. **无痕铁律**：不装证书、不写用户 shell 配置（UV_NO_MODIFY_PATH=1）、规则路由全内存、代理设置运行期改退出恢复
4. **真盲区明确记录**：bash 函数覆盖 curl、pwsh cmdlet，静态分析不可靠，只能 B1/B2

---

## 6. 关联文件

- AIWIKI：`04sessions/260819-agent-mate网络源目标实体表.md`（五层术语基座、目标实体表设计）
- 实测样本：`agent-mate/install.sh`（cnrio.cn uv 0.12.5 定制安装器，72729 字节）、`agent-mate/hermes-install.sh` / `hermes-install.ps1`（hermes 官方原版）

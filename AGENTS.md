# 项目级 AGENTS.md — dsh-easy-up

本文件为项目级规则,替代系统级 AGENTS.md 中与本项目冲突的版本号规则部分。

## 身份认定

你是一个软件工程师,这是一个软件开发项目。

## 版本号规则(本项目专属,替代系统级版本号规则)

本项目使用 `v0.00.000` 格式版本号:

- **大版本**:正式版之前一律为 `v0`。
- **小版本**:两位数字(如 `00`、`01`),占版本号第 2 段。
- **顺序号**:三位数字(如 `001`),占版本号第 3 段,从 `001` 开始,**每次改动 +1,一直递增,不归零**。

### 权限边界

- **大版本、小版本的更新,需要老板的明确指令**,不允许 agent 自主修改。
- **顺序号**:每次改动自动 +1,是 agent 执行写出时自行递增的编号,不需老板逐次批准。

### 应用示例

- 首次修改后版本号: `v0.00.001`
- 下次改动: `v0.00.002`
- 老板明确指示升小版本后: `v0.01.001`(顺序号重新从 001 起随改动递增)
- 正式发布前大版本保持 `v0`。

### 与系统级 AGENTS.md 版本号的关系

- 系统级的 **6 位日期(YYMMDD)** 保持不变;时间戳为 **YYMMDD 6 位**(yymmdd6位数字)。
- **时间戳获取方式**:commit 时用 `date +%y%m%d` 命令获取当前 yymmdd,不得手写。
- 系统级版本号中的 **3 位自然序号,被本项目的 `v0.00.000` 版本号替代**。
- 因此本项目 commit 版本段格式为:`v0.00.000:yymmdd:描述`(时间戳 yymmdd 6 位,取命令值)。
- commit 完整格式示例:`v0.00.000:260816:操作描述`。

### 开发开始前规则

- **开发尚未开始前**版本号保持 `v0.00.000` 不变(补写/更新规则文件等前置工作不递增顺序号);**开发真正开始后**,每次改动才从 `001` 开始 +1。